// Centralized test utility functions and reusable mocks for ProtoFusionGirl tests.
// Add factories, mock classes, and helpers here.

// Example: Phaser mock
export const mockPhaser = () => ({
  // ...mock Phaser API as needed...
});

// Example: EventBus mock
export const mockEventBus = () => ({
  emit: jest.fn(),
  on: jest.fn(),
  off: jest.fn(),
});

// Add more as needed for world state, UI, etc.
